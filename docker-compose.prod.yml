version: '2'

services:
    application:
        container_name: unic_production_application
        restart: always
    workspace:
        container_name: unic_production_workspace
        restart: always
    php-fpm:
        container_name: unic_production_php-fpm
        restart: always
    nginx:
        container_name: unic_production_nginx
        restart: always
        ports:
            - "80:80"
            - "443:443"
    data:
        container_name: unic_production_data
        restart: always
    logs:
        container_name: unic_production_logs
        restart: always
    redis:
        container_name: unic_production_redis
        restart: always
